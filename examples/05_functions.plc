func add(a, b) {
    return a + b;
}

func max(a, b) {
    if (a > b) {
        return a;
    }
    return b;
}

func factorial(n) {
    if (n < 2) {
        return 1;
    }
    return n * factorial(n - 1);
}

print(add(3, 4));
print(max(10, 20));
print(factorial(5));
